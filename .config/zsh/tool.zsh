# ================================
# Tool
# ================================

# Prompt
run_cmd starship 'eval "$(starship init zsh)"'

# Shell Env
run_cmd zoxide 'eval "$(zoxide init zsh)"'

# linuxbrew
# run_cmd /home/linuxbrew/.linuxbrew/bin/brew 'eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv bash)"'

# Node version manager
# run_cmd fnm 'eval "$(fnm env --use-on-cd)"'
